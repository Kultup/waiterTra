# 🎮 Game Scenarios System

Система ігрових сценаріїв дозволяє адміністраторам створювати інтерактивні текстові квести (сценарії) з розгалуженнями, а гравцям — проходити їх через веб-інтерфейс.

## 🏗️ Архітектура

Система складається з трьох основних частин:
1.  **Backend (Node.js/Express + MongoDB)**: Зберігання сценаріїв та згенерованих посилань.
2.  **Game Builder (React)**: Адмін-панель для візуального створення та редагування вузлів гри.
3.  **Game Play (React)**: Інтерфейс гравця для проходження сценаріїв.

### 📊 Структура даних (Schema)

```javascript
// GameScenario
{
  title: String,        // Назва сценарію
  description: String,  // Короткий опис
  startNodeId: String,  // ID початкового вузла
  nodes: [{
    nodeId: String,     // Унікальний ID вузла (генерується на клієнті)
    text: String,       // Текст сцени/опису
    choices: [{
      text: String,     // Текст варіанту відповіді
      nextNodeId: String, // Куди веде цей вибір (null для кінця)
      isWin: Boolean,   // Чи є це перемогою (для кінцевих вузлів)
      result: String    // Результуючий текст фіналу
    }]
  }],
  createdAt: Date
}
```

---

## 🛠️ Game Builder (`GameBuilder.js`)

Основний інструмент для адміністратора. Дозволяє:

### 1. Керування сценаріями
-   **Список сценаріїв**: Перегляд усіх наявних ігор у вигляді карток.
-   **Створення/Видалення**: Додавання нових квестів та видалення застарілих.
-   **Копіювання посилань**: Генерація унікального хеш-посилання для гравця.

### 2. Редактор вузлів (Nodes)
-   **Бічна панель вузлів**: Швидкий перехід між сценами гри.
-   **Початковий вузол**: Можливість призначити будь-який вузол як `START`.
-   **Редагування тексту**: Поле для введення основного опису сцени.

### 3. Логіка виборів (Choices)
-   **Розгалуження**: Кожен вибір може вести до іншого існуючого вузла.
-   **Кінцеві точки**: Якщо вибір веде в "Кінець гри", можна налаштувати статус (Перемога/Програш) та фінальний текст.
-   **Стабільні ID**: Використовуються `choiceId` для коректного рендерингу списків у React.

---

## 🕹️ Game Play (`GamePlay.js`)

Інтерфейс для кінцевого користувача (гравця). Технічні особливості:

### 1. Навігація та Історія
-   **Стек історії**: Система запам'ятовує пройдені вузли, що дозволяє гравцеві повернутися "Назад" (`handleBack`).
-   **Перезапуск**: Можливість миттєвого скидання прогресу та повернення до початку.

### 2. Анімації та UX
-   **Перемикання сцен**: Використання `animKey` для примусового перерендерингу та запуску CSS-анімацій появи тексту та кнопок при кожному переході.
-   **Типографіка**: Використання літер (A, B, C...) для позначення варіантів відповідей для кращої читабельності.

### 3. Фінальні екрани (`GamePlay.css`)
-   Динамічні класи `.win` та `.lose` для візуального підкреслення результату гри.
-   Використання Glassmorphism ефектів та плавних переходів.

---

## 🚀 Як використовувати

1.  Зайдіть у розділ **"Ігрові сценарії"** в адмін-панелі.
2.  Натисніть **"+ Новий сценарій"**.
3.  Створіть вузли, зв'яжіть їх між собою за допомогою виборів.
4.  Збережіть сценарій.
5.  Натисніть на іконку 📋 (Copy) на картці сценарію.
6.  Надішліть посилання гравцеві.

Гравцеві не потрібна реєстрація — він одразу потрапляє у світ вашого сценарію! 🌟
